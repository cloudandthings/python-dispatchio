# Dispatchio configuration — conditions example (local dev)

[dispatchio]
log_level = "INFO"

[dispatchio.state]
backend = "filesystem"
root    = ".dispatchio/state"

[dispatchio.receiver]
backend  = "filesystem"
drop_dir = ".dispatchio/completions"
